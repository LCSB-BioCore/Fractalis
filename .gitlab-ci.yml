image: python:latest

services:
    - redis
    - rabbitmq

before_script:
    - pip install -e .
    - pip install flake8
    - echo "REDIS_HOST='redis'" > /root/f.conf
    - export FRACTALIS_CONFIG=/root/f.conf

test:
    script:
        - python setup.py test
        - flake8 .
